bool checkIfPangram(char* sentence) {
    int freq[26];
    for(int i=0; i<26; i++)
    freq[i]=0;
    for(int i=0; sentence[i]!='\0'; i++)
    freq[sentence[i]-'a']++;
    for(int i=0; i<26; i++){
        if(freq[i]==0)
        return false;
    }
    return true;
}
