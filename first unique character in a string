int firstUniqChar(char* s) {
    int freq[26];
    for(int i=0; i<26; i++) freq[i]=0;
    for(int i=0; s[i]!='\0'; i++) freq[s[i]-'a']++;
    for(int i=0; i<26; i++){
        if(freq[i]==1)
        return i;
    }
    return -1;
}
